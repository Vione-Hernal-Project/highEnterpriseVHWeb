# Legacy Storefront

This folder keeps the original static storefront prototype used as a visual
reference during the Next.js migration.

- Open [index.html](index.html) to compare the old static UI
- Shared media now comes from `../public/assets`
- Active application code lives in `app/`, `components/`, and `lib/`
